// import { Vibrant } from "node-vibrant/browser";

// export async function ImgColorExtracter(url) {
//   const color = await Vibrant.from(url).getPalette();
//   const res = await color;
//   return res;
// }
